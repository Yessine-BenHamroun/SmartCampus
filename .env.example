# Django settings
SECRET_KEY=your-secret-key-here-generate-a-new-one-for-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# MongoDB connection
MONGO_URI=mongodb://localhost:27017/smartcampus
MONGO_DB_NAME=smartcampus

# For production (Render), use MongoDB Atlas URI:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/smartcampus?retryWrites=true&w=majority

# Email Configuration (for password reset)
# For development, use console backend (emails will print in console)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# For production with Gmail:
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=taher.benismail@gmail.com
EMAIL_HOST_PASSWORD=qfwsuxdrcrcirdbn
DEFAULT_FROM_EMAIL=SmartCampus <taher.benismail@gmail.com>

# Note: For Gmail, you need to:
# 1. Enable 2-Factor Authentication
# 2. Generate an App Password at https://myaccount.google.com/apppasswords
# 3. Use the App Password (not your regular password) in EMAIL_HOST_PASSWORD
