"""
Django admin configuration for certifications app
Note: This is for Django's admin panel, but models use MongoDB
"""
from django.contrib import admin

# Register your models here.
# Note: Since we're using MongoDB instead of Django ORM,
# admin integration would require custom admin classes
# For now, use the API endpoints or MongoDB Compass to manage data
